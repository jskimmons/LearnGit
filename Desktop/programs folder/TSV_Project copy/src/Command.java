/**
 * This enumerator holds all of the commands that the
 * user can perform on the data. They are held here to
 * self-document which ones can be used. 
 * @author JoeSkimmons
 * @version 1.0
 */

public enum Command {
		COUNT, MIN, MAX, SUM, AVG;
}
