/**
 * This enumerator holds all of the commands that the
 * user can perform on the data. They are held here to
 * self-document which ones can be used, and the prevent
 * the user from choosing one that does
 * not exist.
 * @author JoeSkimmons, jws2191
 * @version 1.0
 */

public enum Command {
		COUNT, MIN, MAX, SUM, AVG;
}
